# =======================================================
# CONFIGURATION D'ENVIRONNEMENT - TEMPLATE
# =======================================================
# 
# Ce fichier est un exemple (.env.example).
# Copiez-le en .env.local et personnalisez les valeurs.
# =======================================================

# CONFIGURATION BASE DE DONNÉES MYSQL
DB_NAME=your_database
DB_HOST=your_host
DB_USER=your_user
DB_PASSWORD=your_password
DB_PORT=3306

# CONFIGURATION APPLICATION
APP_ENV=dev

# EMAIL / SMTP (optionnel mais recommandé en prod)
# Adresse d'expéditeur par défaut
MAIL_FROM=no-reply@localhost
MAIL_FROM_NAME=EcoRide

# Configuration SMTP: si SMTP_HOST est défini, l'envoi utilisera SMTP
# Exemples pour un relai SMTP standard
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# tls | ssl | (laisser vide pour désactiver)
SMTP_SECURE=tls
