# =======================================================
# CONFIGURATION DEV — EXEMPLE
# =======================================================
# Copier ce fichier en .env.local pour un environnement de dev local.
# Vous pouvez ensuite adapter les valeurs selon votre setup.
# =======================================================

# APPLICATION
APP_ENV=dev
SITE_URL=http://localhost:8080/
APP_TZ=Europe/Paris

# MYSQL LOCAL (via docker-compose)
DB_NAME=ecoride
DB_HOST=mysql
DB_USER=ecoride
DB_PASSWORD=ecoride
DB_PORT=3306

# MONGO LOCAL (optionnel)
MONGO_DSN=mongodb://mongo:27017
MONGO_DB=ecoride

# EMAIL — Adresse d'expéditeur dev
MAIL_FROM=no-reply@dev.local
MAIL_FROM_NAME=EcoRide (Dev)
MAIL_HOSTNAME=dev.local
MAIL_REPLY_TO=

# SMTP — Dev (exemples)
# Gmail SMTP (utilisez un mot de passe d’application):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre.adresse@gmail.com
# SMTP_PASS=motdepasse_application
# SMTP_SECURE=tls

# Sinon, laissez SMTP_HOST vide: en dev, les e-mails sont journalisés dans /tmp/ecoride-mail.log
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=tls
SMTP_DEBUG=0

# LIST-UNSUBSCRIBE (optionnel)
LIST_UNSUBSCRIBE_URL=
LIST_UNSUBSCRIBE_MAILTO=
LIST_UNSUBSCRIBE_POST=0

# DKIM (optionnel si votre relais ne signe pas)
# Soit un chemin (file://) vers une clé privée PEM, soit le contenu direct de la clé
DKIM_DOMAIN=
DKIM_SELECTOR=
DKIM_PRIVATE_KEY=
DKIM_PASSPHRASE=

# RÈGLES MÉTIER
RIDE_CREATE_FEE_CREDITS=2
